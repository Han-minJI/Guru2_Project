<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/DANO"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- 배경 설정 -->
    <RelativeLayout
        android:id="@+id/starttext"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#272D4F"
        android:clipToOutline="true">

        <!-- 타이틀 텍스트 -->
        <TextView
            android:id="@+id/dano"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="180dp"
            android:layout_marginBottom="170dp"
            android:fontFamily="@font/rubiks"
            android:gravity="center"
            android:text="@string/dano"
            android:textAppearance="@style/dano" />

        <FrameLayout
            android:id="@+id/group_wrapper"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_below="@id/dano"
            android:layout_marginTop="50dp"> <!-- 타이틀과의 간격 -->

            <!-- 사각형 드로어블 배경 -->

            <!-- 시작하기 텍스트 -->
            <ImageButton
                android:id="@+id/startDANO"
                android:layout_width="328dp"
                android:layout_height="57dp"
                android:background="@drawable/rectangle_1" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:gravity="center"
                android:text="시작하기"
                android:textAppearance="@style/fontnavy" />
        </FrameLayout>




    </RelativeLayout>
</FrameLayout>
